# 安装：pip install -r requirements.txt

# ---- LangChain 全家桶 ----
langchain==1.3.14              # 应用框架（含 core / text-splitters / classic）
langchain-chroma==1.1.0        # Chroma 向量库集成
langchain-huggingface==1.2.2   # HuggingFaceEmbeddings（bge-m3 加载）
langchain-openai==1.4.2        # ChatOpenAI（DeepSeek 走 OpenAI 兼容接口）

# ---- 嵌入模型底层 ----
sentence-transformers==5.7.0   # bge-m3 推理所需

# ---- 前端 / API ----
streamlit==1.61.1              # Web 页面
fastapi==0.141.1               # REST API 框架
uvicorn==0.52.1                # ASGI 服务器
httpx==0.28.1                  # FastAPI TestClient 测试依赖

# ---- 文档解析 ----
pypdf==6.16.1                  # PDF 文本提取
python-docx==1.2.0             # Word 段落/表格提取

# ---- 配置加载 ----
python-dotenv==1.2.2           # .env 密钥加载
